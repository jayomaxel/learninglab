# LinguistFlow AI 产品详细介绍文档

## 1. 产品概述

**LinguistFlow AI**（又名 LearningLab）是一款专为高效语言学习设计的智能化 Web 应用。它结合了最先进的 **Google Gemini AI** 技术与现代教育心理学（如间隔重复、RSVP 阅读法），为语言初学者到资深学习者提供全方位的听、读、记体验。

该产品目前支持 **英语 (EN)**、**法语 (FR)** 和 **韩语 (KR)**，并深度集成 AI 能力，能够根据用户的 CEFR 等级（A0 到 C2）动态生成和分析学习内容。

---

## 2. 核心功能模块

### 2.1 任务中心 (Mission Center)
*   **每日目标**：设定量化的单词记忆、听力及阅读目标。
*   **进度追踪**：直观展示当日成就，助力用户建立长效学习回路。

### 2.2 听力实验室 (Listening Lab)
*   **原生多模态素材**：利用 Gemini 原生音频模态输出 PCM 流，实现高自然度的听力材料生成。
*   **自适应听写 (Flow Sync)**：系统实时感应用户录入节奏，支持 0.5x 极致降速下的音质补偿。
*   **难点预热**：AI 在练习前自动扫描并提取核心词汇，进行“先扫盲再训练”的闭环。

### 2.3 极速阅读器 (Speed Reader)
*   **RSVP 阅读引擎**：通过高频定焦（ORP）算法，大幅提升单位时间内的信息摄取效率。
*   **智能素材抓取**：解析任意 URL 或主题，由 AI 进行语义分块（Chunking）并适配当前语言等级。

### 2.4 智能生词本 (Vocabulary Bank)
*   **全场景收藏**：听力、阅读、查词一键汇聚，AI 自动补全释义、语法标签及音标。
*   **科学复习调度**：基于改进的 SM-2 算法，配套“复习雪崩平滑”机制，确保复习压力恒定。

---

## 3. 核心技术详解 (Technical Deep-Dive)

### 3.1 AI 音频流式链路与对齐 (Audio Pipeline)
*   **原生多模态流 (Native Modality)**：直接调用 Gemini 2.x 音频能力，跳过“文本转语音”延迟，实现从 LLM 语义到发音起伏的深度耦合。
*   **跨语言权重校准 (Phonetic Weighting)**：
    *   **法语 (FR)**：引入“音节补偿因子”，自动识别并校准法语连读（Liaison）对时长比例的影响。
    *   **韩语 (KR)**：基于“字核 (Syllable blocks)”而非空格计算发音权重，确保对齐误差控制在 50ms 以内。
*   **极致滤波 (Noise Gate)**：在 AudioBufferSourceNode 后级挂载自研 **EnvelopeFollower 节点**，实时监控 RMS 电平。若检测到模型由于幻觉生成的背景噪音或异常停顿，自动应用线性淡出，确保听感纯净。

### 3.2 自适应速率控制与性能 (Flow Sync & Performance)
*   **命令式帧同步 (Bypassing VDOM)**：通过 `requestAnimationFrame` 直接对底层 `HTMLAudioElement` 进行命令式操控，实现 60fps 的实时响应，规避虚拟 DOM 渲染瓶颈。
*   **移动端“刺客”防御**：
    *   **系统级集成**：深度调用 **MediaSession API**，确保在 iOS Safari 锁屏状态下 AudioContext 不被挂起，并提供系统级进度控制。
    *   **性能回退机制**：当监测到移动设备发热或 CPU 负载过高时，系统自动将自定义音频节点链路降级为原生 MediaElement 驱动，保障基础学习体验的稳定性。

### 3.3 语义级分级控制 (CEFR Guardrails)
*   **混合校验机制**：在 Prompt 约束基础上，引入离线“词频白名单”过滤层。对 AI 生成内容进行静态扫描。
*   **非阻塞 UI 补偿 (UX Optimization)**：针对检测出的“超纲词”，系统不再发起冗余的 LLM 请求进行二次替换，而是通过 **视觉引导（镂空下划线）** 进行标记，结合本地 Wasm 词典提供非侵入式释义。

---

## 4. 数据同步与容错 (Resilience & Sync)

### 4.1 离线优先与同步策略
*   **异步请求队列 (Request Queue)**：AI 生成任务存储在 IndexedDB 的 `PendingActions` 队列中。在无网环境下切换至本地轻量级 Fallback 词典（Wasm + SQLite）。
*   **状态合并 (Logical Clocks)**：针对多设备离线复习可能产生的冲突，引入 **混合逻辑时钟 (Hybrid Logical Clocks)** 与序列号校验机制，确保在网络恢复后能精准合并多端学习状态，遵循“最后写入者胜 (LWW)”与“遗忘感知识别”原则。

### 4.2 复习雪崩平滑 (Backlog Smoothing)
*   **遗忘惩罚梯度**：当用户断更产生大量逾期复习时，算法会应用一个 `0.7x ~ 0.9x` 的强度衰减权重。
*   **随机打散 (Rescheduling)**：将逾期任务平滑分布在未来 3-5 天的任务中，防止单一爆发式复习量击退用户学习热情。

---

## 5. 设计哲学

LinguistFlow AI 的核心目标是 **“消除认知阻力”**：
1.  **掩盖生成延迟**：利用“预热测试”掩盖 AI 推理时间；利用分块流式处理提升首句响应速度。
2.  **定焦对齐算法**：Flexbox 定焦 ORP 消除视觉抖动。
3.  **人文关怀技术**：通过 Flow Sync 将听力练习从“被动听”转变为“主动拼”，让学习过程更具游戏化快感。

---

## 6. 总结

LinguistFlow AI 不仅是一款语言学习应用，更是一次关于 **LLM 多模态能力高度产品化** 的深度实践。它通过底层音频工程和科学的学习模型，真正实现了将庞大的大模型能力转化为具体、可量化、且具备极致交互体验的个人学习资产。
